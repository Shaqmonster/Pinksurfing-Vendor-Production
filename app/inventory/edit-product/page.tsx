import EditProductClient from "../editProduct/[id]/edit-product-client";

/** Static export — single HTML file at /inventory/edit-product (no dynamic segment). */
export default function EditProductPage() {
  return <EditProductClient />;
}
